# Groq cloud inference API key (Tier 1 voice parsing fallback).
# Get one at https://console.groq.com/keys — free tier, no credit card.
# Leave empty to force Tier 0 (offline pattern matching) only.
VITE_GROQ_API_KEY=
